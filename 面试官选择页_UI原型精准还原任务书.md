# 面试官选择页 UI 原型精准还原任务书

> 使用方式：将本文件与“当前页面截图”“目标原型截图”以及 6 张透明背景人物 PNG 一并提供给编码 AI。  
> 本任务是**原型还原**，不是自由设计。AI 必须严格按本文档执行，不得自行发挥。

---

## 0. 任务身份与最终目标

你现在是一名“前端 UI 还原工程师”，不是产品经理，也不是视觉设计师。

你的唯一目标是：

> 在不破坏现有业务功能的前提下，将当前项目中的“选择应聘岗位 / 选择面试官”页面，准确还原为目标 UI 原型的结构、尺寸、视觉层级和交互状态。

参考资料的优先级如下：

1. **目标 UI 原型截图**：视觉唯一标准；
2. **本文档**：实现约束和验收标准；
3. **现有项目代码**：业务逻辑、接口、路由和技术栈依据；
4. 当前页面截图：仅用于识别差异，不作为目标设计依据。

如本文档与目标原型截图存在轻微差异，以目标原型截图为准；如目标截图无法判断业务逻辑，以现有代码为准，不得猜测。

---

## 1. 开始修改前必须完成的检查

先分析，不要立即重写代码。

### 1.1 阅读项目

必须先阅读并确认：

- `AGENTS.md`、`README.md` 或项目开发说明；
- `package.json`；
- 当前页面对应的路由文件；
- 当前页面组件；
- 面试官卡片组件；
- 页面使用的状态管理、接口请求和提交逻辑；
- 全局样式、主题变量和 UI 组件库；
- 当前人物图片和图标资源目录。

### 1.2 确认技术栈

根据项目现状继续开发，例如：

- Vue 项目继续使用 Vue；
- React 项目继续使用 React；
- TypeScript 项目不得改成 JavaScript；
- 已使用组件库时优先复用，但不得为了复用而偏离原型；
- 不得无故新增大型依赖。

### 1.3 建立基线

修改前必须：

1. 记录当前涉及的文件；
2. 检查 `git status`；
3. 不覆盖与本页面无关的未提交修改；
4. 启动项目并确认当前页面可以打开；
5. 在固定视口下保存一张修改前截图；
6. 确认当前选择岗位、选择面试官、上一步和进入面试按钮是否正常。

推荐视觉验收视口：

```text
宽度：1440px
高度：900px
浏览器缩放：100%
```

若项目已有设计基准视口，则沿用项目标准，但整个修改过程必须使用同一个视口。

---

## 2. 不允许破坏的现有功能

本次以 UI 改造为主。除非目标原型确实要求，否则禁止修改以下内容：

- 页面路由地址；
- 接口 URL；
- 请求方法和参数结构；
- 登录与权限逻辑；
- 岗位列表的获取方式；
- 面试官选择值的业务字段；
- 表单校验规则；
- “上一步”按钮行为；
- “进入面试”按钮提交行为；
- 全局状态管理；
- 错误提示机制；
- 其他页面的样式和功能。

必须遵守：

```text
先复用现有逻辑，再替换展示层。
不得为了做 UI 而重写完整页面业务。
不得删除暂时看不懂的代码。
不得自行修改接口返回数据。
不得自行编造接口字段。
```

如果目标原型需要 6 位人物，而现有接口只返回 4 种面试模式：

1. 先检查项目是否已有 6 位人物配置；
2. 如已有，使用现有数据；
3. 如没有，建立独立的前端展示配置，并保留原业务 ID 映射；
4. 不得静默改变后端协议；
5. 在最终报告中说明新增了哪些展示配置和映射。

---

## 3. 目标页面信息架构

页面从上到下必须由四个稳定区域组成：

```text
页面
├── 顶部导航栏
│   ├── 左侧：返回
│   └── 右侧：步骤进度
├── 主内容区
│   ├── 选择应聘岗位
│   ├── 岗位选择框
│   ├── 分割线
│   ├── 选择面试官
│   └── 3 列 × 2 行人物卡片
└── 底部操作栏
    ├── 上一步
    └── 进入面试
```

### 3.1 页面整体

目标效果：

- 页面高度至少铺满一个视口；
- 顶部导航和底部操作区为白色；
- 中间主体是非常浅的蓝色、青色和淡紫色渐变；
- 主内容集中在页面中间，不得像当前页面一样横向拉得过宽；
- 页面整体干净、轻量，不使用重阴影和高饱和大色块；
- 视觉重点是岗位选择框和人物面试官卡片。

推荐结构：

```css
.page {
  min-height: 100vh;
  display: flex;
  flex-direction: column;
}

.page-header {
  flex: 0 0 auto;
}

.page-main {
  flex: 1 1 auto;
}

.page-footer {
  flex: 0 0 auto;
}
```

不得使用大量无意义的绝对定位拼整页。

---

## 4. 目标视觉尺寸与布局

以下数值是基于原型比例给出的实现基准。可以根据项目基础字号做 1～3px 微调，但不允许改变整体比例。

### 4.1 顶部导航栏

- 高度：约 `48px`；
- 背景：白色或接近白色；
- 下边界：极浅分割线；
- 左侧返回入口距离页面左边约 `30px`；
- 右侧步骤进度距离页面右边约 `28px`；
- 返回文字简洁，图标与文字垂直居中；
- 步骤 1 已完成，显示蓝色勾选；
- 步骤 2 为当前步骤，显示蓝色圆形数字 `2`；
- 两步之间为细蓝线；
- 不得把步骤进度放到主内容区。

### 4.2 主内容容器

目标原型在桌面端是窄内容区，不是满宽表单。

建议：

```css
.content-container {
  width: min(710px, calc(100vw - 48px));
  margin: 0 auto;
}
```

约束：

- 所有标题、说明文字、选择框、分割线和卡片网格左边缘对齐；
- 主内容距顶部导航约 `18px`；
- 内容区不得设置成 1000px 以上；
- 大屏下不得自动拉伸卡片；
- 小于桌面宽度时允许自然收缩，但本轮优先保证桌面原型。

### 4.3 主体背景

推荐使用多层柔和渐变，效果应接近原型：

```css
.page-main {
  background:
    radial-gradient(circle at 12% 20%, rgba(196, 241, 255, 0.70), transparent 34%),
    radial-gradient(circle at 78% 12%, rgba(232, 221, 255, 0.58), transparent 38%),
    linear-gradient(180deg, #f7fbff 0%, #f9fbff 100%);
}
```

要求：

- 左侧略偏浅青；
- 右上略偏浅紫；
- 中下部接近白色；
- 颜色必须柔和；
- 不得使用明显条带；
- 不得使用深蓝背景；
- 不得增加纹理、网格或粒子动画。

### 4.4 标题和说明文字

“选择应聘岗位”和“选择面试官”：

- 字号建议：`16px`；
- 字重建议：`600`；
- 颜色：深灰黑；
- 不得做成 24px 以上的大标题。

说明文字：

- 字号建议：`12px`；
- 颜色：中浅灰蓝；
- 与标题间距约 `4px`；
- 行高舒适，不要过密。

### 4.5 岗位选择框

- 宽度：占满内容容器；
- 高度：约 `44px`；
- 白色背景；
- 圆角：约 `10px`；
- 边框极浅，不能有明显深色描边；
- 输入文字在左，箭头在右；
- 下拉行为继续使用现有组件和逻辑；
- 禁止将其改成搜索弹窗或多选框；
- 选择框下方到分割线约 `16px`。

### 4.6 分割线

- 宽度与内容容器一致；
- 颜色极浅；
- 上下留白清楚；
- 不得使用粗线或虚线。

---

## 5. 面试官卡片区

### 5.1 网格

桌面原型必须是：

```text
第一行：3 张
第二行：3 张
```

推荐：

```css
.interviewer-grid {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  column-gap: 20px;
  row-gap: 40px;
}
```

尺寸基准：

- 单张卡片宽度：约 `223px`；
- 单张卡片可见高度：约 `115px`；
- 卡片圆角：`10px`～`12px`；
- 第一行距区域标题约 `42px`，需要给溢出人物头像留空间；
- 第二行与第一行之间留出人物头部和视觉呼吸空间；
- 六张卡片宽高必须一致；
- 禁止出现第一张特别宽或第二行只有一张孤立在左侧的当前布局。

### 5.2 卡片结构

每张卡片必须是一个可点击的单选卡片：

```text
卡片
├── 左侧文字区
│   ├── 姓名
│   ├── 类型标签
│   └── 一行特征描述
└── 右侧透明人物半身图
```

必须删除当前卡片右侧的“大圆形勾选按钮”视觉。

选中状态通过以下方式表达：

- 蓝色细边框；
- 类型标签为蓝底白字；
- 可选：极轻蓝色外发光；
- 不得再增加大号勾选圆圈。

未选中状态：

- 卡片白色；
- 无明显边框或仅极浅边框；
- 标签浅灰底、灰色字；
- 不得整体降低到难以阅读；
- 人物图片保持正常显示。

### 5.3 卡片文字

姓名：

- 字号建议：`14px`；
- 字重：`600`；
- 颜色：深灰黑。

类型标签：

- 高度约 `23px`；
- 左右内边距约 `8px`；
- 圆角约 `5px`；
- 字号约 `12px`；
- 文本不换行。

描述：

- 字号建议：`11px`～`12px`；
- 单行展示优先；
- 超出卡片时省略号；
- 左侧可使用很小的浅蓝三角形或箭头；
- 不得使用项目符号占据过多空间。

文字区域必须预留人物位置，避免遮挡：

```css
.card-copy {
  position: relative;
  z-index: 2;
  width: 58%;
  min-width: 0;
}
```

### 5.4 人物图片

人物素材必须使用透明背景 PNG 或 WebP。

要求：

- 使用半身或上半身职业人物；
- 放在卡片右侧；
- 底部贴合卡片底部；
- 头部允许超出卡片顶部约 `20px`～`28px`；
- 图片宽度建议 `96px`～`116px`；
- 不得裁成圆形；
- 不得强制拉伸；
- 不得改变人物脸部比例；
- 不得加白色矩形底；
- 不得给人物单独加边框；
- 不得遮挡姓名和标签。

推荐：

```css
.interviewer-card {
  position: relative;
  min-height: 115px;
  overflow: visible;
}

.interviewer-avatar {
  position: absolute;
  z-index: 1;
  right: 8px;
  bottom: 0;
  width: 108px;
  max-height: 142px;
  object-fit: contain;
  object-position: bottom right;
  pointer-events: none;
  user-select: none;
}
```

人物脚部或下半身可使用非常轻的白色渐隐，使其自然融入卡片，但不得产生明显白雾遮挡文字。

### 5.5 六位人物展示数据

如目标原型文案即为正式数据，按以下内容展示：

| 姓名 | 类型标签 | 特征描述 |
|---|---|---|
| 李越 | 专业严谨型 | 逻辑严谨、不冷不热、中立客观 |
| 陆晓宇 | 压力拷问型 | 气场强、爱打断、喜欢反问质疑 |
| 李雪 | 引导耐心型 | 包容耐心、善于引导、提问温和 |
| 孙小岚 | 轻松亲和型 | 提问生活化、不刻板教条 |
| 王博洋 | 发散开放型 | 看重逻辑思维、解决问题能力 |
| 刘彬 | HR综合型 | 资深HRBP、重人品、情商 |

注意：

- 展示姓名不应直接作为后端业务 ID；
- 每一项应具有稳定 `id`；
- 若后端已有 ID，建立映射；
- 若页面数据来自接口，不得为了写死以上文案而破坏接口；
- 图片路径集中配置，不要散落在模板中。

推荐数据结构：

```ts
export interface InterviewerOption {
  id: string;
  businessValue: string;
  name: string;
  type: string;
  description: string;
  avatar: string;
}

export const interviewerOptions: InterviewerOption[] = [
  {
    id: "li-yue",
    businessValue: "professional",
    name: "李越",
    type: "专业严谨型",
    description: "逻辑严谨、不冷不热、中立客观",
    avatar: "/assets/interviewers/li-yue.png"
  }
];
```

如实际项目字段不同，适配现有类型定义，不得复制出第二套互相冲突的状态。

---

## 6. 底部操作栏

- 位于页面底部；
- 白色背景；
- 顶部有极浅分割线；
- 高度约 `49px`～`56px`；
- 两个按钮水平居中；
- “上一步”为白底浅灰边框；
- “进入面试”为蓝色主按钮；
- 两个按钮高度一致；
- 按钮不应过大；
- 图标与文字保持居中；
- 不得悬浮到卡片区域之上；
- 主内容过高时应自然滚动，底栏实现方式必须避免遮挡最后一行卡片。

推荐：

```css
.page-footer {
  min-height: 52px;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 14px;
  border-top: 1px solid rgba(18, 39, 68, 0.08);
  background: rgba(255, 255, 255, 0.96);
}
```

是否固定定位应根据现有页面结构选择。若使用 `position: fixed`，必须给主体增加对应底部空间，禁止遮挡内容。

---

## 7. 交互要求

### 7.1 面试官选择

- 一次只能选择一位面试官；
- 点击整张卡片都可选择；
- 键盘用户可以通过 Tab 聚焦卡片；
- Enter 或 Space 可选择；
- 已选中卡片要有明确视觉状态；
- 不得因为点击人物透明区域而失效；
- 现有表单值必须正确更新。

### 7.2 进入面试按钮

继续沿用现有校验逻辑。

如当前规则是必须选择岗位和面试官：

- 未满足条件时按钮保持禁用或显示现有错误提示；
- 满足条件后正常提交；
- 不得仅为了视觉效果删除禁用状态；
- 不得修改跳转目标。

### 7.3 响应式

本轮桌面端为主。

最低要求：

- 宽度不足时内容区不产生横向滚动；
- 允许在较窄屏幕下变为 2 列或 1 列；
- 不得影响 1440×900 桌面原型；
- 不需要自行设计新的移动端视觉方案。

推荐断点：

```css
@media (max-width: 860px) {
  .interviewer-grid {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }
}

@media (max-width: 560px) {
  .interviewer-grid {
    grid-template-columns: 1fr;
  }
}
```

---

## 8. 颜色和样式基准

优先复用项目现有主题色。若项目无统一变量，可使用以下基准：

```css
:root {
  --ui-primary: #3f5cff;
  --ui-primary-hover: #304be8;
  --ui-primary-soft: rgba(63, 92, 255, 0.10);

  --ui-text-main: #1f2937;
  --ui-text-secondary: #7b8798;
  --ui-text-muted: #a5adba;

  --ui-card-bg: rgba(255, 255, 255, 0.94);
  --ui-border-light: rgba(31, 41, 55, 0.08);
  --ui-divider: rgba(31, 41, 55, 0.07);

  --ui-tag-bg: #eef0f3;
  --ui-tag-text: #9aa2ad;
}
```

原则：

- 蓝色只用于当前步骤、选中卡片和主按钮；
- 不得出现多种互相冲突的蓝色；
- 不使用纯黑大面积文字；
- 不使用重阴影；
- 卡片阴影最多为极轻柔阴影；
- 圆角保持一致，不得每个组件一个圆角值。

---

## 9. 推荐组件拆分

根据现有项目结构适配，不得为了拆分而过度重构。

推荐最小组件：

```text
InterviewSetupPage
├── StepHeader
├── JobSelector
├── InterviewerGrid
│   └── InterviewerCard
└── PageActions
```

原则：

- 页面负责数据和状态；
- 卡片负责展示与点击；
- 人物配置集中管理；
- 样式优先使用当前项目约定；
- 不要把所有 CSS 写成大量行内样式；
- 不要创建与本任务无关的通用组件库。

---

## 10. 必须分阶段执行

禁止一次性重写整个页面。按以下顺序完成：

### 阶段 A：分析

输出：

- 当前页面文件位置；
- 当前状态和接口逻辑；
- 当前卡片结构；
- 与原型的差异；
- 计划修改的文件；
- 潜在风险。

此阶段不修改代码。

### 阶段 B：页面骨架

只处理：

- 顶部导航；
- 主体背景；
- 内容区宽度；
- 底部操作栏；
- 标题与选择框的整体位置。

完成后运行项目并截图检查。

### 阶段 C：卡片网格

只处理：

- 3 列 × 2 行；
- 卡片宽高；
- 行列间距；
- 页面垂直节奏。

完成后再次截图检查。

### 阶段 D：单张卡片

先完成一张卡片的：

- 姓名；
- 标签；
- 描述；
- 人物定位；
- 选中状态。

确认结构正确后，再复用到全部六张。

### 阶段 E：接入数据和交互

- 对接现有岗位数据；
- 对接面试官业务值；
- 保持单选；
- 保持提交和返回逻辑；
- 处理默认值和禁用状态。

### 阶段 F：视觉微调

只允许调整：

- 间距；
- 字号；
- 人物大小；
- 边框；
- 渐变；
- 对齐。

不得在此阶段重构业务。

### 阶段 G：测试和验收

执行：

- lint；
- typecheck；
- build；
- 已有测试；
- 页面功能回归；
- 固定视口截图；
- 检查控制台错误。

---

## 11. 人物素材要求

目标 UI 是否准确，人物素材非常关键。

期望目录示例：

```text
src/assets/interviewers/
├── li-yue.png
├── lu-xiaoyu.png
├── li-xue.png
├── sun-xiaolan.png
├── wang-boyang.png
└── liu-bin.png
```

素材规范：

- PNG 或 WebP；
- 透明背景；
- 人物比例统一；
- 上半身职业形象；
- 清晰度足够；
- 每张图片画布留白尽量一致；
- 不得用截图中带白底的人物矩形直接放入卡片。

若素材缺失：

- 不得擅自从网络下载陌生人物；
- 不得使用表情包或卡通头像替代；
- 不得假装已经精准还原；
- 先完成布局并使用统一占位资源；
- 在最终报告中明确列出缺失素材；
- 等待用户补充正式人物图后再替换。

---

## 12. 验收标准

以下条件必须同时满足。

### 12.1 视觉验收

- [ ] 主内容明显比当前页面窄，并居中；
- [ ] 页面背景接近浅青到浅紫的柔和渐变；
- [ ] 顶部返回和步骤进度位置与原型一致；
- [ ] 岗位选择框横跨整个内容区；
- [ ] 分割线位置正确；
- [ ] 面试官为 3 列 × 2 行；
- [ ] 六张卡片宽高一致；
- [ ] 人物从卡片顶部自然溢出；
- [ ] 人物位于右侧，文字位于左侧；
- [ ] 选中卡片使用蓝色细边框；
- [ ] 未选中卡片标签为浅灰；
- [ ] 页面中不存在当前版本的大圆形勾选图标；
- [ ] 底部按钮居中；
- [ ] 页面没有明显错位、溢出或遮挡。

### 12.2 功能验收

- [ ] 岗位下拉仍可使用；
- [ ] 只能选择一位面试官；
- [ ] 选中状态与表单值一致；
- [ ] 上一步仍可返回；
- [ ] 进入面试仍按原逻辑提交；
- [ ] 未选择必要项时仍有校验；
- [ ] 页面刷新后不存在运行错误；
- [ ] 控制台无新增 error；
- [ ] 其他页面没有被全局样式污染。

### 12.3 工程验收

- [ ] 没有无关依赖；
- [ ] 没有重写接口层；
- [ ] 没有大面积修改无关文件；
- [ ] TypeScript 无新增类型错误；
- [ ] lint 通过；
- [ ] build 通过；
- [ ] 所有资源路径有效；
- [ ] 不存在未使用的旧卡片样式；
- [ ] 不存在重复维护的面试官状态。

---

## 13. 视觉对比和自动检查

如果项目可以使用 Playwright，增加或执行固定视口截图测试：

```ts
import { test, expect } from "@playwright/test";

test("interviewer selection page visual", async ({ page }) => {
  await page.setViewportSize({ width: 1440, height: 900 });
  await page.goto("请替换为实际页面路由");

  await expect(page).toHaveScreenshot(
    "interviewer-selection-page.png",
    {
      fullPage: true,
      animations: "disabled"
    }
  );
});
```

若不适合增加测试文件，至少要：

1. 固定视口打开页面；
2. 保存修改后截图；
3. 并排对照目标原型；
4. 按具体差异进行最后一次调整；
5. 不要用“看起来差不多”作为验收结论。

对比时重点检查：

- 内容区宽度；
- 标题起始位置；
- 选择框高度；
- 第一行卡片垂直位置；
- 三列卡片宽度；
- 人物头部高度；
- 第二行间距；
- 底栏高度和按钮位置。

---

## 14. 严格禁止事项

以下行为视为任务失败：

- 自行重新设计页面；
- 将目标原型改成个人认为更好看的版本；
- 修改接口协议；
- 修改路由；
- 删除已有校验；
- 删除上一步或进入面试逻辑；
- 把人物改为圆形头像；
- 继续保留当前的大圆形勾选按钮；
- 把六张卡片做成横向滚动；
- 使用随机网络人物；
- 给卡片增加复杂动效；
- 增加粒子、光效、网格背景；
- 使用过重阴影；
- 页面主体铺满超宽屏；
- 一次修改大量无关文件；
- 未运行项目就声称完成；
- build 失败仍声称任务完成；
- 素材缺失时声称已像素级还原。

---

## 15. 每次修改时的行为规则

每轮只解决明确问题。

收到新的视觉反馈时，例如：

```text
1. 内容区过宽；
2. 卡片高度过高；
3. 人物太大；
4. 第二行距离太近；
```

只修复以上四项。

不得顺便：

- 改颜色；
- 改文案；
- 改按钮；
- 重构组件；
- 更换图标；
- 调整接口。

每次修改后必须查看 `git diff`，确认没有扩大修改范围。

---

## 16. 最终交付格式

完成后只输出以下内容，不要写泛泛的总结：

```md
## 实现结果

### 修改文件
- 文件路径：修改内容
- 文件路径：修改内容

### 保留的业务逻辑
- 岗位选择：
- 面试官选择：
- 上一步：
- 进入面试：

### 视觉实现
- 页面宽度：
- 卡片布局：
- 人物定位：
- 选中状态：
- 底部操作栏：

### 测试结果
- lint：
- typecheck：
- build：
- 页面回归：
- 控制台错误：

### 尚未完成
- 无 / 明确列出缺失人物素材或无法验证的内容

### 需要用户确认
- 仅列出确实需要确认的问题
```

不得只回复“已经完成”“已按照原型优化”。

---

# 最终执行指令

请严格按以下顺序工作：

1. 阅读项目和参考文件；
2. 找到当前页面、组件、数据和业务逻辑；
3. 先输出差异分析和修改计划；
4. 不得在分析阶段直接重写代码；
5. 按阶段 B～G 逐步实现；
6. 每个阶段都运行页面检查；
7. 以目标 UI 原型截图为视觉唯一标准；
8. 保持现有接口、路由、校验和提交逻辑；
9. 缺少人物素材时必须明确说明，不得擅自替换；
10. 完成后运行 lint、typecheck、build 和页面回归；
11. 用固定视口截图与目标原型对比；
12. 最后按第 16 节格式汇报。

**本任务不是自由设计。任何未在原型、本文档或现有业务代码中出现的内容，都不得自行添加。**

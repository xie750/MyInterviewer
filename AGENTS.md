# AGENTS.md

本文件是 AI 模拟面试网站仓库的长期开发规则。Codex、Claude Code 或其他 AI 开发代理进入本仓库时，必须先阅读本文件，再阅读当前任务相关文档和代码。

## 1. 项目定位

本项目是一个 Spring Boot 3 + Vue 3 前后端分离的 AI 模拟面试网站，目标功能来自 `任务三：AI模拟面试网站开发(1).md`：

- 账号密码登录和 JWT 鉴权；
- 普通用户、管理员双角色权限隔离；
- 岗位选择、面试官风格选择、AI 多轮面试；
- 面试评分、总结报告和历史记录；
- 简历临时解析；
- 语音/文字双模式面试；
- 虚拟人面试官展示与素材管理；
- 浏览器端摄像头姿态检测与姿态报告；
- 管理员后台管理用户、岗位、面试记录、虚拟人素材和姿态配置。

开发顺序必须遵循 Harness 版本规划：先完成可运行、可测试的 MVP，再逐步增加进阶功能。

## 2. 必读顺序

每次开始开发前，按以下顺序读取上下文：

1. `AGENTS.md`
2. `docs/product/current-scope.md`
3. 与任务相关的 `docs/architecture/*.md`
4. 当前版本文档，如 `docs/releases/v0.1-mvp.md`
5. 当前变更单，如 `docs/changes/CR-xxx.md`
6. `docs/tasks/current-task.md`
7. 相关代码和测试

如果文档与代码冲突，以真实代码和可运行测试为准，同时更新受影响文档。

## 3. 范围控制

- 只实现 `docs/tasks/current-task.md` 明确要求的内容。
- 路线图中的未来功能不等于当前可以开发。
- 不为了方便一次性提前实现后续版本能力。
- 不复制整套版本文档；维护“当前有效主文档 + 版本增量 + 变更单 + 当前任务”。
- 涉及新增功能时，变更文档必须写明：当前行为、目标行为、新增内容、修改内容、保持不变、本次不做、异常降级、验收标准。

## 4. 技术约束

### 后端

- 使用 Spring Boot 3，建议 JDK 17 和 Maven。
- 鉴权采用 JWT；所有业务接口必须经过登录校验。
- 管理员接口必须有后端权限校验，不能只依赖前端隐藏入口。
- 大模型调用必须统一封装服务层，不能在 Controller 中散落第三方 API 调用。
- 简历解析结果默认仅用于当前面试会话，除非变更文档明确要求，否则不得保存原始简历文件。
- 数据库变更通过迁移脚本演进，已发布迁移不直接改写。
- 业务上必须采用三层架构，Controller层，Service层，Mapper层。

### 前端

- 使用 Vue 3 + TypeScript + Vite。
- 路由和页面必须做登录态与角色权限拦截。
- UI 必须以设计稿为准，重点适配 1080P 下浏览器 80%、100%、125% 缩放。
- 虚拟人窗口、摄像头预览窗口必须保持比例稳定，不得遮挡核心面试内容。
- 语音、摄像头、虚拟人资源不可用时必须降级，不得阻断基础文字面试流程。

### AI 面试

- 面试官风格、岗位提示词、追问策略应配置化。
- AI 问答要保存足够上下文以支持多轮追问和报告生成。
- MVP 阶段优先保证文字面试完整可用；语音和虚拟人不改变原有文字流程。

## 5. 权限与隐私红线

- 普通用户只能访问自己的面试记录、报告和姿态结果。
- 普通用户不能访问管理员后台、全站记录、虚拟人配置和姿态阈值配置。
- 管理员可以查看和管理全站数据，但也必须通过后端权限校验。
- 摄像头原始视频流不得上传后端，不得保存。
- 姿态检测尽量在浏览器本地完成，后端只接收结构化检测结果。
- 简历原文件和临时解析缓存应在面试结束后销毁或过期。
- 日志中不得输出用户密码、JWT、API Key、简历全文或敏感视频数据。

## 6. 降级规则

进阶能力必须有可用兜底：

- 语音识别失败：允许文字输入。
- 语音合成失败：只展示文字问题。
- 麦克风权限拒绝：切换文字面试。
- 摄像头权限拒绝：关闭姿态检测，不阻断面试。
- 姿态模型加载失败：记录不可用状态，不影响面试主流程。
- 虚拟人加载失败：展示静态面试官形象或占位状态。
- 简历解析失败：允许用户不使用简历继续面试。

## 7. 测试与验收

每次修改后至少考虑以下验证：

- 后端编译和单元/接口测试；
- 前端类型检查、Lint、构建或组件测试；
- 关键权限回归：未登录、普通用户越权、用户访问他人数据；
- 关键流程回归：登录、创建面试、回答、追问、结束、生成报告；
- 涉及 UI 时检查 1080P 下 80%、100%、125% 缩放；
- 涉及媒体能力时检查权限拒绝和资源加载失败场景。

如果因为项目阶段尚未具备测试命令，必须在最终说明中注明未运行原因，并在文档中记录待建立的测试基线。

## 8. 文档更新规则

- 当前真实实现变化后，更新 `docs/product/current-scope.md`。
- 架构、数据库、接口或权限变化后，更新对应 `docs/architecture/*.md`。
- 版本范围变化后，更新 `docs/releases/*.md` 和 `docs/product/roadmap.md`。
- 每次任务开始前更新 `docs/tasks/current-task.md`。
- 每次任务完成后，将结果记录到 `docs/tasks/completed.md` 或当前任务文档中。

## 9. 代码协作规则

- 保持改动小而清晰，不做无关重构。
- 优先遵循现有代码风格、目录结构和命名方式。
- 新增公共能力前先检查是否已有工具、服务或组件可复用。
- 不提交密钥、真实账号、个人隐私数据或本地环境专属路径。
- 不删除用户已有改动；如果发现工作区有无关变更，默认保留。


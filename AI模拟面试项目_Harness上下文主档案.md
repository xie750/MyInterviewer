# AI 模拟面试网站：Harness 工程上下文主档案

> 用途：切换到新的 AI 对话窗口时，将本文件作为项目背景提供给 AI，使其快速理解项目目标、当前决策、版本规划和下一步工作。
>
> 当前阶段：项目规划期，尚未正式开始完整代码开发；准备先设计 Harness 工程文档并实施 MVP。

---

## 1. 项目是什么

项目名称暂定为“AI 模拟面试网站”。

项目采用 **Spring Boot 3 + Vue 3 前后端分离架构**，目标是实现一套包含 AI 面试、简历解析、语音交互、虚拟人面试官、摄像头姿态检测、面试报告和管理员后台的综合面试训练平台。

用户的完整使用流程为：

> 登录 → 上传简历 → 选择岗位 → 选择面试官风格 → 开启摄像头 → AI 虚拟人提问 → 用户语音或文字回答 → AI 智能追问 → 结束面试 → 生成面试评价报告和姿态检测报告。

### 1.1 普通用户功能

- 登录系统；
- 上传并临时解析简历；
- 选择应聘岗位；
- 选择面试官风格；
- 进行文字或语音 AI 面试；
- 查看 AI 评分和总结报告；
- 查看个人面试历史；
- 查看个人姿态检测报告。

### 1.2 管理员功能

- 用户管理；
- 岗位管理；
- 面试官风格及提示词管理；
- 查看全站面试记录；
- 查看全站姿态异常记录；
- 虚拟人素材管理；
- 姿态检测阈值配置。

### 1.3 隐私约束

- 简历只用于当前面试，不应长期保存原文件；
- 摄像头原始画面不得上传或保存；
- 姿态识别尽量在浏览器本地运行；
- 后端只保存结构化姿态异常结果；
- 用户只能查看自己的面试和姿态数据；
- 管理员接口必须实施严格的后端权限校验。

---

## 2. 对 Harness 工程的统一理解

Harness 工程不等于某个具体框架，也不只是写需求文档。

在本项目中，Harness 工程指一套帮助 Codex、Claude Code 等 AI 稳定开发项目的工程约束，包括：

1. **长期上下文**：项目目标、技术栈、架构和固定规则；
2. **当前状态**：系统目前已经实现什么、尚未实现什么；
3. **版本增量**：下一版本在原系统基础上增加或修改什么；
4. **当前任务**：AI 本次只能完成哪一个小任务；
5. **边界约束**：哪些功能本次不做、哪些旧功能不能破坏；
6. **自动验证**：通过编译、测试、Lint 和回归测试判断是否完成；
7. **变更记录**：记录数据库、接口和架构为什么发生变化。

核心公式：

> AI 当前上下文 = 长期规则 + 当前系统状态 + 本版本目标 + 本次变更说明 + 当前任务 + 可执行验收。

Harness 的目标不是让 AI 一次生成更多代码，而是让 AI 始终知道：

- 当前系统是什么样；
- 本次要改变什么；
- 哪些内容不能改变；
- 怎样证明任务已经完成。

---

## 3. 已确定的开发原则

### 3.1 先完成 MVP，再逐步增加进阶功能

第一版不直接实现所有数字人、语音和姿态检测功能，而是先完成可运行、可测试的基础业务闭环。

### 3.2 每个版本建立在上一版本的稳定基线上

每个版本完成后必须：

- 更新当前系统状态；
- 更新实际架构、接口和数据库文档；
- 补充测试；
- 运行原有回归测试；
- 标记完成内容和遗留问题。

### 3.3 不为每个版本复制整套文档

不采用 `V1全部文档/V2全部文档/V3全部文档` 的方式，因为会形成多个相互矛盾的信息源。

采用：

> 一套当前有效的主文档 + 各版本增量说明 + 具体变更单 + 当前任务 + 测试。

### 3.4 进阶功能必须提供降级方案

- 语音不可用 → 切换文字面试；
- 麦克风权限被拒绝 → 允许文字输入；
- 摄像头不可用 → 关闭姿态检测，但不阻断面试；
- 数字人加载失败 → 展示静态面试官；
- 简历解析失败 → 允许不使用简历继续面试。

---

## 4. MVP 第一版范围

建议 MVP 版本号为 `v0.1`。

### 4.1 MVP 必须实现

- 账号密码登录；
- JWT 登录鉴权；
- 普通用户和管理员双角色权限；
- 岗位列表与岗位选择；
- 面试官风格选择；
- 创建面试会话；
- 文字版 AI 提问；
- 用户文字回答；
- AI 根据历史对话智能追问；
- 结束面试；
- 自动生成评分和总结报告；
- 用户查看个人历史记录；
- 管理员查看全站面试记录；
- 基础用户管理和岗位管理。

### 4.2 MVP 暂不实现

- 简历上传和大模型解析；
- 用户语音识别；
- AI 语音合成；
- 摄像头姿态检测；
- 2D/3D 数字人；
- 数字人口型同步；
- 复杂动画和完整素材后台。

### 4.3 MVP 核心闭环

> 登录 → 选择岗位 → 选择面试官 → 文字 AI 面试 → AI 追问 → 结束面试 → 生成报告 → 查看历史记录。

### 4.4 MVP 测试基线

第一版至少要建立以下回归测试：

- 正确账号可以登录；
- 错误密码无法登录；
- 未登录用户无法访问业务接口；
- 普通用户无法访问管理员接口；
- 用户只能查看自己的面试记录；
- 用户可以完成一次文字面试；
- AI 可以基于上下文追问；
- 面试结束后可以生成报告；
- 管理员可以查看全站记录；
- 前后端构建通过。

---

## 5. 后续版本路线图

| 版本 | 主要目标 | 与上一版的关系 |
|---|---|---|
| `v0.1` | 登录、权限、岗位、文字面试、报告 | 建立基础闭环 |
| `v0.2` | 简历上传与临时解析 | 为 AI 面试增加个性化上下文 |
| `v0.3` | 语音识别与语音合成 | 保留文字模式作为降级方案 |
| `v0.4` | 摄像头姿态检测 | 独立运行，失败时不阻断面试 |
| `v0.5` | 数字人基础展示 | 不改变原有 AI 面试逻辑 |
| `v0.6` | 数字人口型、动作与状态切换 | 扩展数字人表现能力 |
| `v0.7` | 管理后台进阶功能 | 增加素材、姿态记录与阈值管理 |
| `v1.0` | UI 适配、完整测试、部署交付 | 形成最终完整版本 |

版本号可以根据实际情况调整，但应保持“基础闭环优先、进阶能力后置”的顺序。

---

## 6. 推荐的 Harness 文档目录

```text
ai-interview-platform/
├── AGENTS.md
├── README.md
├── .env.example
├── docker-compose.yml
│
├── docs/
│   ├── product/
│   │   ├── product-overview.md
│   │   ├── current-scope.md
│   │   ├── roadmap.md
│   │   └── user-flow.md
│   │
│   ├── architecture/
│   │   ├── system-architecture.md
│   │   ├── database-design.md
│   │   ├── api-contract.md
│   │   ├── permission-design.md
│   │   ├── ai-interview-design.md
│   │   └── privacy-design.md
│   │
│   ├── releases/
│   │   ├── v0.1-mvp.md
│   │   ├── v0.2-resume.md
│   │   ├── v0.3-voice.md
│   │   ├── v0.4-posture.md
│   │   └── v0.5-avatar.md
│   │
│   ├── changes/
│   │   ├── CR-001-resume-parsing.md
│   │   ├── CR-002-voice-interview.md
│   │   ├── CR-003-posture-detection.md
│   │   └── CR-004-avatar.md
│   │
│   ├── tasks/
│   │   ├── backlog.md
│   │   ├── current-task.md
│   │   └── completed.md
│   │
│   └── decisions/
│       ├── ADR-001-frontend-posture-detection.md
│       └── ADR-002-temporary-resume-storage.md
│
├── backend/
│   ├── AGENTS.md
│   ├── pom.xml
│   └── src/
│
├── frontend/
│   ├── AGENTS.md
│   ├── package.json
│   └── src/
│
├── database/
│   ├── schema.sql
│   ├── seed.sql
│   └── migrations/
│
├── tests/
│   ├── api/
│   └── e2e/
│
└── scripts/
    └── verify.*
```

---

## 7. 每类文档的职责

| 文档 | 解决的问题 | 更新时机 |
|---|---|---|
| `AGENTS.md` | AI 在本仓库中必须遵守什么长期规则 | 稳定规则发生变化时 |
| `README.md` | 项目是什么、如何运行 | 环境或启动方式变化时 |
| `product-overview.md` | 产品整体目标和用户是谁 | 产品方向变化时 |
| `current-scope.md` | 当前系统真实实现了什么 | 每个版本完成后 |
| `roadmap.md` | 后续版本准备增加什么 | 计划调整时 |
| `system-architecture.md` | 当前系统怎样分层和交互 | 实际架构变化后 |
| `database-design.md` | 当前数据库实体和关系 | 表结构变化后 |
| `api-contract.md` | 当前有效的前后端接口约定 | 接口变化后 |
| `releases/v0.x.md` | 某个版本整体增加什么 | 版本启动和完成时 |
| `changes/CR-xxx.md` | 某个功能如何改变现有系统 | 开发该功能前 |
| `tasks/current-task.md` | AI 本次只做什么 | 每次任务开始前 |
| `decisions/ADR-xxx.md` | 为什么选择某个关键技术方案 | 重大决策确定后 |
| 自动测试 | 新功能是否完成、旧功能是否被破坏 | 每次开发后运行 |

重要原则：

- `AGENTS.md` 只放长期稳定规则，不放本周任务和临时需求；
- `current-scope.md` 只记录当前真实状态，不保存过时设计；
- `roadmap.md` 中的未来功能不代表 AI 当前可以开发；
- `current-task.md` 是本次开发范围的最终约束。

---

## 8. 第一版和第二版如何让 AI 看明白

不要重新复制一套 V2 全量文档。第二版开始时，AI 应按以下顺序读取：

1. `AGENTS.md`：长期开发规则；
2. `docs/product/current-scope.md`：第一版已经完成什么；
3. 相关架构文档：当前系统如何实现；
4. `docs/releases/v0.2-xxx.md`：第二版整体目标；
5. `docs/changes/CR-xxx.md`：具体功能如何改变第一版；
6. `docs/tasks/current-task.md`：本次只开发哪一小步；
7. 相关代码与测试：验证文档与真实实现是否一致。

每份变更文档必须明确：

### 8.1 当前行为

第一版目前是怎样工作的。

### 8.2 目标行为

增加新功能以后应当怎样工作。

### 8.3 新增内容

需要增加哪些页面、接口、服务、字段或数据表。

### 8.4 修改内容

哪些已有模块需要改变。

### 8.5 保持不变

哪些已有功能、接口、权限和业务流程不得被破坏。

### 8.6 本次不做

明确排除不属于本次版本的功能，防止 AI 扩大范围。

### 8.7 异常与降级

第三方服务、摄像头、麦克风或模型不可用时如何处理。

### 8.8 验收标准

用可以执行或观察的条件判断功能是否完成。

---

## 9. 版本增量文档示例

以 `v0.3` 增加语音面试为例：

```md
# v0.3 语音面试版本

## 前置版本
基于已经通过验收的 v0.2 开发。

## 当前行为
- AI 问题以文字显示；
- 用户通过输入框回答；
- 文字面试流程已经完整可用。

## 目标行为
- AI 问题可以自动播放语音；
- 用户可以录音并转写为文字；
- 用户确认转写内容后，继续调用原有面试逻辑。

## 本版本新增
- 浏览器录音；
- 语音转文字；
- 文字转语音；
- 录音与播放状态管理。

## 保持不变
- 原有文字输入继续可用；
- 原有 AI 追问逻辑保持不变；
- 原有评分报告逻辑保持不变；
- 用户权限规则保持不变。

## 本版本不做
- 数字人口型同步；
- 姿态检测；
- 语音情绪分析。

## 降级要求
- 用户拒绝麦克风权限时切换到文字模式；
- 语音识别失败时允许重新录制或手动输入；
- AI 语音生成失败时只显示文字，不中断面试。

## 验收标准
- 正常情况下可以完成语音面试；
- 语音失败时仍能完成文字面试；
- 原有文字面试回归测试全部通过。
```

---

## 10. 当前任务文档模板

每次只让 AI 完成一个小而清晰的任务：

```md
# 当前任务

## 任务编号
VOICE-02

## 任务名称
实现前端浏览器录音组件

## 项目上下文
- 当前稳定版本：v0.2
- 当前开发版本：v0.3
- 文字面试已经完整可用

## 本次实现
1. 请求麦克风权限；
2. 开始和停止录音；
3. 显示录音状态；
4. 返回录音文件或音频数据。

## 本次不实现
- 语音识别接口；
- AI 语音播放；
- 数字人口型同步；
- 姿态检测。

## 保持不变
- 原有文字输入组件继续可用；
- 不修改后端面试逻辑；
- 不改变面试消息数据结构。

## 完成标准
1. 用户允许权限后可以录音；
2. 用户拒绝权限时显示提示；
3. 拒绝权限不影响文字面试；
4. 前端类型检查、测试和构建通过；
5. 不破坏现有面试页面。
```

---

## 11. 推荐技术架构方向

以下为当前建议，并非所有细节都已最终确定：

| 部分 | 建议技术 |
|---|---|
| 后端 | Spring Boot 3、JDK 17 |
| 前端 | Vue 3、TypeScript、Vite |
| 状态管理 | Pinia |
| UI 组件 | Element Plus 或根据设计稿自定义 |
| 数据库 | MySQL 8 |
| 鉴权 | JWT + 前端路由拦截 + 后端接口鉴权 |
| API 文档 | OpenAPI/Swagger |
| AI 调用 | 统一封装大模型服务层 |
| 姿态检测 | 浏览器端 JavaScript 人体姿态模型 |
| 虚拟人 | 先静态/2D，后续再升级口型和动作 |
| 测试 | 后端单元/接口测试、前端组件测试、关键流程 E2E |

仍需在正式搭建工程前确认：

- 后端采用 MyBatis-Plus 还是 JPA；
- 使用 Maven 还是 Gradle，当前更建议 Maven；
- 大模型供应商及 API；
- 语音识别和语音合成供应商；
- 数字人技术方案；
- UI 设计稿和组件规范；
- 是否使用 Redis；
- 项目最终部署环境。

---

## 12. 数据库初步实体

第一版及后续可能涉及：

| 数据表 | 作用 |
|---|---|
| `sys_user` | 用户、密码、状态和角色 |
| `job_position` | 岗位、技术栈、难度和提示词模板 |
| `interviewer_style` | 面试官风格和专属提示词 |
| `interview_session` | 单场面试的基本信息和状态 |
| `interview_message` | AI 与用户的问答消息 |
| `interview_report` | 评分和总结报告 |
| `posture_event` | 姿态异常事件，后续版本新增 |
| `posture_config` | 姿态检测阈值，后续版本新增 |
| `virtual_human` | 数字人素材配置，后续版本新增 |

主要关系：

- 一个用户可以拥有多场面试；
- 一场面试对应一个岗位和一种面试官风格；
- 一场面试包含多条消息；
- 一场面试生成一份综合报告；
- 一场面试可以产生多条姿态异常事件。

数据库变更应采用迁移脚本逐步演进，例如：

```text
V001__initial_schema.sql
V002__add_resume_context.sql
V003__add_voice_fields.sql
V004__add_posture_tables.sql
V005__add_virtual_human_tables.sql
```

已发布的旧迁移脚本原则上不直接修改，通过新迁移记录版本变化。

---

## 13. AI 开发时的标准工作流程

每次交给 AI 开发时，要求它执行：

1. 阅读 `AGENTS.md`；
2. 阅读当前状态、版本和变更文档；
3. 检查相关代码和测试；
4. 总结已有能力；
5. 列出新增、修改、保持不变和不在范围内的内容；
6. 给出小范围实施计划；
7. 只修改当前任务需要的文件；
8. 补充或修改相应测试；
9. 运行新功能测试和旧功能回归测试；
10. 更新受影响的当前状态、接口和架构文档；
11. 最后报告修改文件、测试结果、风险和遗留事项。

推荐的新窗口开发提示词：

```md
请先阅读我提供的项目上下文和仓库中的 AGENTS.md，不要立即编写代码。

然后依次检查：
1. docs/product/current-scope.md
2. 相关 architecture 文档
3. 当前 release 文档
4. 当前 change request
5. docs/tasks/current-task.md
6. 相关代码与测试

请先输出：
- 当前系统已经实现什么；
- 本次需要新增和修改什么；
- 哪些旧功能必须保持不变；
- 哪些内容不属于本次范围；
- 计划运行哪些测试。

确认上下文一致后再开始实施。本次只完成 current-task.md 中的任务，不提前开发路线图中的后续功能。完成后运行相关测试和回归测试，并更新已经受到影响的当前有效文档。
```

---

## 14. 需求、代码和测试的追踪方式

建议每项需求设置稳定编号，例如：

```text
AUTH-01    用户登录
AUTH-02    JWT校验
AUTH-03    管理员权限隔离
INT-01     创建面试会话
INT-02     AI首次提问
INT-03     用户文字回答
INT-04     AI上下文追问
RPT-01     生成面试报告
VOICE-01   麦克风权限
VOICE-02   浏览器录音
POSTURE-01 用户离开画面检测
AVATAR-01  数字人基础展示
```

建立追踪表：

| 需求编号 | 代码模块 | 测试 | 状态 |
|---|---|---|---|
| `AUTH-01` | 登录接口和登录页面 | 登录成功/失败测试 | 待开发 |
| `AUTH-03` | 后端权限校验 | 普通用户越权测试 | 待开发 |
| `INT-04` | AI 面试服务 | 多轮上下文测试 | 待开发 |
| `VOICE-02` | 录音组件 | 录音状态测试 | 后续版本 |

通过编号把需求、代码、测试和完成状态对应起来，避免出现“文档说完成，但没有代码或测试”的情况。

---

## 15. 当前尚未完成的事项

截至本上下文文档生成时，只完成了需求分析和 Harness 方法设计，尚未完成以下内容：

- 尚未创建正式 Git 项目；
- 尚未搭建 Spring Boot 和 Vue 工程；
- 尚未最终确定详细技术选型；
- 尚未编写正式 `AGENTS.md`；
- 尚未编写 MVP 产品、架构、数据库和接口文档；
- 尚未建立任务编号和追踪表；
- 尚未编写代码；
- 尚未建立自动测试和回归测试基线。

因此，新窗口不应误认为项目已经开发完成。

---

## 16. 下一步建议

下一窗口建议直接从“Harness 第一版文档设计”开始，而不是马上写所有业务代码。

推荐顺序：

1. 最终确认 MVP 范围；
2. 确认基础技术栈；
3. 生成根目录 `AGENTS.md`；
4. 生成 `product-overview.md`；
5. 生成 `current-scope.md`；
6. 生成 `roadmap.md`；
7. 生成 `v0.1-mvp.md`；
8. 设计系统架构；
9. 设计数据库表和关系；
10. 定义接口规范；
11. 设计权限模型；
12. 将 MVP 拆分为可执行任务；
13. 配置测试与验收命令；
14. 再开始搭建代码工程。

建议向新窗口提出的第一条任务：

> 请基于本上下文主档案，先帮我完成 AI 模拟面试项目的 MVP Harness 文档设计。不要直接写完整业务代码。请先确定文档清单和相互关系，然后依次生成根目录 AGENTS.md、产品概述、当前范围、版本路线图和 v0.1 MVP 版本说明，并保证这些文档之间没有重复和冲突。

---

## 17. 最重要的结论

1. Harness 工程不仅是上下文文档，还包括任务边界、测试验证和版本变更记录。
2. 第一版和第二版之间不复制整套文档，而是通过“当前主文档 + 版本增量 + 变更单 + 当前任务 + 回归测试”衔接。
3. `AGENTS.md` 存放长期稳定规则，不能塞入所有临时需求。
4. `current-scope.md` 是当前系统真实状态的唯一入口。
5. `release` 文档描述一个版本整体增加什么。
6. `change request` 描述某项功能如何改变已有系统。
7. `current-task.md` 限定 AI 本次只能做什么。
8. 每次增加新功能都必须写明“当前行为、目标行为、保持不变、本次不做、降级方案、验收标准”。
9. 每个版本完成后要更新主文档并建立测试基线。
10. 当前应先完成 MVP Harness 文档设计，再搭建和开发代码。


# 模拟面试页面 UI 原型精准还原执行文档

> 使用对象：Codex、Claude Code、Cursor、Windsurf、Trae、GitHub Copilot Agent 等可读取项目代码并修改文件的 AI 编程工具。  
> 项目技术栈：Vue 3 + TypeScript + Vite。  
> 使用方式：将本文件、目标 UI 原型截图、当前项目页面截图一并交给 AI。  
> 截图命名要求：
>
> - `目标原型图`：设计稿或 UI 原型截图，是唯一视觉标准。
> - `当前实现图`：当前项目页面截图，只用于定位差距，不能作为设计依据。

---

## 1. AI 的任务身份

你现在是一名负责前端 UI 还原的高级工程师。你的任务不是重新设计页面，也不是根据个人审美优化页面，而是：

**在保留现有业务功能、接口、状态管理和交互逻辑的前提下，将当前模拟面试页面的布局、尺寸、间距、层级、颜色、圆角、字体和状态展示，尽可能准确地还原为目标 UI 原型。**

本次任务的优先级从高到低如下：

1. 页面整体结构与区域比例；
2. 组件位置和尺寸；
3. 间距、对齐和视觉层级；
4. 颜色、圆角、边框、阴影；
5. 字号、行高、图标和细节；
6. 响应式适配；
7. 动画和过渡效果。

**禁止为了增加功能而破坏原型。禁止以“功能已经实现”为理由保留错误布局。**

---

## 2. 视觉标准与信息优先级

发生冲突时，严格按照以下优先级执行：

1. 目标 UI 原型截图；
2. 本文档中的尺寸和结构要求；
3. 项目现有业务逻辑；
4. 当前项目代码；
5. 当前实现截图；
6. AI 自己的判断。

目标原型图是视觉真值。当前实现图只用于判断哪里做错了。

AI 不得凭空补充以下内容：

- 原型中不存在的新按钮；
- 原型中不存在的新卡片；
- 原型中不存在的标题；
- 原型中不存在的装饰；
- 原型中不存在的背景渐变；
- 原型中不存在的功能入口；
- 自行设计的悬浮工具栏；
- 自行改变的信息层级。

原型没有明确展示的内容，应优先隐藏、折叠或保持现状，不得擅自放大展示。

---

## 3. 本次改造范围

### 3.1 允许修改

允许修改与 UI 还原直接相关的内容：

- 页面 DOM 结构；
- Vue 组件拆分；
- CSS、SCSS、Less；
- 布局方式；
- 组件尺寸；
- 组件间距；
- 颜色变量；
- 圆角；
- 边框；
- 阴影；
- 图标位置；
- 文本排版；
- 局部滚动区域；
- 响应式规则；
- 无视频时的占位状态；
- 次要功能的折叠和收纳方式；
- 页面样式相关的 class 命名。

### 3.2 禁止修改

除非代码无法运行，否则禁止修改：

- 后端接口；
- 接口参数；
- 路由地址；
- 登录逻辑；
- 面试流程；
- 题目生成逻辑；
- AI 对话逻辑；
- 语音识别逻辑；
- 语音播报逻辑；
- 摄像头权限逻辑；
- WebSocket 逻辑；
- Pinia/Vuex 状态结构；
- 数据库存储逻辑；
- 已存在的类型定义；
- 与当前页面无关的其他页面；
- package.json 中无关依赖；
- 项目目录结构的大规模重构。

不得删除现有功能。无法放入原型主界面的次要功能，应收纳到“更多设置”或折叠区域中。

---

## 4. 当前页面存在的核心问题

根据目标原型图与当前实现图，当前页面至少存在以下问题：

### 4.1 页面整体骨架不一致

目标原型是：

- 顶部固定导航栏；
- 主体分为左侧视频舞台和右侧面试信息面板；
- 左侧视频区域居中；
- 右侧面板固定宽度；
- 页面整体无浏览器纵向滚动。

当前实现存在：

- 左侧上方大面积留白；
- 视频卡片靠近页面底部；
- 视频卡片尺寸过大；
- 右侧面板内容过多；
- 整个网页出现纵向滚动条；
- 页面更像功能调试页，不像正式面试页面。

### 4.2 视频区域比例错误

目标原型中的两个视频窗口：

- 水平排列；
- 大小接近；
- 使用横向矩形比例；
- 整体位于左侧区域视觉中心；
- 不随浏览器宽度无限放大。

当前实现中的视频窗口过大，并且占位头像被放大为整张插画，破坏了视频窗口的真实感。

### 4.3 右侧信息层级错误

目标原型右侧面板只突出：

- 面试官信息；
- 当前问题；
- 当前状态；
- 结束面试。

当前实现将听写、播报、手动模式、摄像头检测、姿态检测、文本输入等功能全部平铺，导致信息密度过高。

### 4.4 滚动区域错误

长问题不应把整个页面撑高。

正确行为：

- 页面主体固定在一屏内；
- 右侧面板固定；
- 长问题只在问题卡片内部滚动；
- 次要设置区域内部可以滚动；
- 浏览器页面本身不滚动。

---

## 5. 目标页面结构

页面必须重构为以下结构：

```text
InterviewPage
├── InterviewHeader
│   ├── BackButton
│   └── InterviewStatus
│
└── InterviewMain
    ├── InterviewStage
    │   └── VideoGrid
    │       ├── InterviewerVideoCard
    │       │   ├── VideoOrPlaceholder
    │       │   ├── NameBadge
    │       │   └── MicrophoneButton
    │       │
    │       └── CandidateVideoCard
    │           ├── VideoOrPlaceholder
    │           ├── NameBadge
    │           └── MicrophoneButton
    │
    └── InterviewPanel
        ├── InterviewerProfile
        ├── Divider
        ├── PanelContent
        │   ├── QuestionCard
        │   ├── ListeningStatus
        │   └── OptionalMoreSettings
        └── PanelFooter
            └── EndInterviewButton
```

建议组件拆分：

```text
src/views/interview/InterviewPage.vue
src/components/interview/InterviewHeader.vue
src/components/interview/InterviewStage.vue
src/components/interview/VideoCard.vue
src/components/interview/InterviewPanel.vue
src/components/interview/InterviewerProfile.vue
src/components/interview/QuestionCard.vue
src/components/interview/ListeningStatus.vue
src/components/interview/InterviewActions.vue
```

如当前项目已有类似组件，应优先复用和调整，不要机械创建重复组件。

---

## 6. 页面尺寸与布局要求

### 6.1 全局页面

页面必须占满整个浏览器视口：

```css
html,
body,
#app {
  width: 100%;
  height: 100%;
  margin: 0;
  overflow: hidden;
}
```

页面根节点：

```css
.interview-page {
  width: 100vw;
  height: 100vh;
  overflow: hidden;
}
```

禁止：

- 页面整体出现纵向滚动条；
- 使用固定的超大高度；
- 使用 `min-height: 1200px`；
- 使用大量绝对定位拼页面；
- 使用 `transform: scale()` 缩放整个页面；
- 使用浏览器缩放模拟设计稿。

### 6.2 顶部导航栏

目标要求：

- 高度：48px；
- 宽度：100%；
- 左侧显示返回；
- 右侧显示绿色圆点和“面试进行中”；
- 垂直居中；
- 左右内边距约 24px；
- 背景比主体略亮；
- 底部存在很弱的分割线。

参考：

```css
.interview-header {
  height: 48px;
  padding: 0 24px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  box-sizing: border-box;
}
```

### 6.3 主体两列布局

主体必须使用 CSS Grid：

```css
.interview-main {
  height: calc(100vh - 48px);
  padding: 16px;
  box-sizing: border-box;
  display: grid;
  grid-template-columns: minmax(0, 1fr) 364px;
  gap: 16px;
}
```

要求：

- 左侧占据剩余空间；
- 右侧固定约 364px；
- 两侧间距约 16px；
- 主体四周内边距约 16px；
- 左右区域都不能撑破视口。

### 6.4 左侧视频舞台

左侧区域必须使用 Flex 居中：

```css
.interview-stage {
  min-width: 0;
  min-height: 0;
  display: flex;
  align-items: center;
  justify-content: center;
}
```

视频区域不能靠底，不能靠顶，不能通过大额 `margin-top` 调整位置。

### 6.5 视频网格

目标要求：

- 两个视频窗口水平排列；
- 整体最大宽度约 790px；
- 两个卡片之间约 16px；
- 在左侧区域水平、垂直居中；
- 大屏下不能无限放大。

```css
.video-grid {
  width: 100%;
  max-width: 790px;
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 16px;
}
```

根据实际视口高度，可使用：

```css
.video-grid {
  max-width: min(790px, calc((100vh - 130px) * 1.45));
}
```

### 6.6 视频卡片

每个视频卡片：

- 使用约 4:3 比例；
- 圆角约 14px；
- 隐藏溢出；
- 视频或图片铺满；
- 使用 `object-fit: cover`；
- 底部左侧显示名称；
- 底部右侧显示麦克风按钮。

```css
.video-card {
  position: relative;
  aspect-ratio: 4 / 3;
  overflow: hidden;
  border-radius: 14px;
  background: #202734;
}

.video-card video,
.video-card img {
  width: 100%;
  height: 100%;
  display: block;
  object-fit: cover;
}
```

禁止：

- 将头像插画直接拉伸铺满视频卡片；
- 视频卡片使用固定 600px 或 700px 高度；
- 视频卡片占满整个左侧；
- 视频未开启时出现大片文字堆叠。

### 6.7 无视频占位状态

面试官没有视频流时：

- 使用深色背景；
- 头像居中；
- 头像建议为 96px 至 128px；
- 头像使用圆形或与原型一致的卡片；
- 不得将头像放大成全屏插画。

候选人摄像头未开启时：

- 中间显示摄像头图标；
- 下方显示简短状态；
- 可提供一个轻量开启入口；
- 不得在卡片中堆叠调试信息。

### 6.8 右侧面试面板

面板必须：

- 高度填满主体区域；
- 使用纵向 Flex；
- 头部固定；
- 中部可滚动；
- 底部按钮固定；
- 圆角约 12px；
- 内边距约 16px。

```css
.interview-panel {
  min-width: 0;
  min-height: 0;
  height: 100%;
  padding: 16px;
  box-sizing: border-box;
  display: flex;
  flex-direction: column;
  border-radius: 12px;
  background: #232832;
}

.panel-header {
  flex-shrink: 0;
}

.panel-content {
  flex: 1;
  min-height: 0;
  padding-top: 16px;
  overflow-y: auto;
}

.panel-footer {
  flex-shrink: 0;
  padding-top: 16px;
}
```

### 6.9 当前问题卡片

问题卡片：

- 使用深蓝色背景；
- 圆角约 12px；
- 内边距约 16px；
- 字号约 14px；
- 行高约 1.7；
- 最大高度约 220px 至 260px；
- 内容超出时只在卡片内部滚动。

```css
.question-card {
  padding: 16px;
  border-radius: 12px;
  background: #23314a;
  max-height: 260px;
  overflow-y: auto;
  font-size: 14px;
  line-height: 1.75;
}
```

禁止让问题内容直接撑高整个右侧面板。

### 6.10 状态区域

目标原型中的状态应简洁展示，例如：

```text
● 正在聆听…
```

要求：

- 小面积状态标签；
- 绿色状态点；
- 文字不超过一行；
- 不显示大块调试信息；
- 不同时展示多个相互冲突的状态。

### 6.11 结束面试按钮

按钮放在面板底部：

- 宽度 100%；
- 高度约 34px 至 40px；
- 红色文字；
- 红色弱边框；
- 深色或透明背景；
- 不出现两个重复的“结束面试”按钮。

---

## 7. 次要功能的处理方式

当前项目已有以下功能时，不得删除，但不要全部平铺在正式面试主界面：

- 开始听写；
- 停止听写；
- 播报问题；
- 手动模式；
- 自动模式；
- 摄像头检测；
- 姿态检测；
- 字幕；
- 文本输入回答；
- 调试状态；
- 麦克风权限信息；
- 摄像头权限信息。

处理规则：

### 7.1 主界面直接保留

主界面只直接展示：

- 当前问题；
- 当前听取状态；
- 麦克风快捷按钮；
- 摄像头快捷按钮；
- 结束面试。

### 7.2 收纳到“更多设置”

以下内容放入可折叠的“更多设置”：

- 播报模式；
- 手动/自动模式；
- 姿态检测开关；
- 字幕开关；
- 输入方式切换；
- 调试信息。

“更多设置”默认收起，不得抢占主视觉。

### 7.3 文本回答输入框

如果产品必须支持文本回答：

- 默认折叠；
- 或在用户切换到“文字回答”模式后出现；
- 不得始终占据右侧底部大面积空间；
- 出现后仍需保证结束面试按钮可见。

---

## 8. 视觉变量建议

统一使用 CSS 变量，禁止在多个组件中重复散落相近颜色：

```css
.interview-page {
  --page-bg: #171c24;
  --header-bg: #222730;
  --panel-bg: #232832;
  --card-bg: #202734;
  --question-bg: #23314a;
  --text-primary: #f4f6fa;
  --text-secondary: #9ca6b5;
  --border-subtle: rgba(255, 255, 255, 0.06);
  --success: #42dd8b;
  --danger: #df5865;
}
```

颜色最终以目标原型截图为准。上述颜色只作为初始值，不得机械照搬后停止对比。

---

## 9. 响应式要求

### 9.1 主要验收分辨率

必须重点保证：

- 1366 × 768；
- 1440 × 900；
- 1920 × 1080。

### 9.2 中等宽度

当视口宽度小于约 1100px：

- 右侧面板可缩小到 320px；
- 视频区域仍然保持两个卡片；
- 必须优先缩小视频区，而不是让页面滚动。

### 9.3 小屏策略

当视口宽度小于约 900px：

- 可以切换为上下布局；
- 右侧面板放到视频区下方；
- 页面可根据产品需要允许局部滚动；
- 不得在桌面宽度下提前切换为上下布局。

示例：

```css
@media (max-width: 1100px) {
  .interview-main {
    grid-template-columns: minmax(0, 1fr) 320px;
  }
}

@media (max-width: 900px) {
  html,
  body,
  #app {
    overflow: auto;
  }

  .interview-page {
    min-height: 100vh;
    height: auto;
    overflow: visible;
  }

  .interview-main {
    height: auto;
    grid-template-columns: 1fr;
  }

  .interview-panel {
    min-height: 560px;
  }
}
```

---

## 10. AI 修改代码前必须执行的分析流程

AI 不得直接修改代码。必须先完成以下步骤。

### 第一步：定位页面入口

输出：

- 当前面试页面路由；
- 页面入口文件；
- 使用到的主要组件；
- 页面样式文件；
- 状态管理文件；
- 摄像头逻辑位置；
- 语音逻辑位置；
- 问题展示逻辑位置。

### 第二步：建立“原型—代码”映射

必须输出表格：

| 原型区域 | 当前代码位置 | 当前问题 | 计划修改方式 | 是否影响业务逻辑 |
|---|---|---|---|---|
| 顶部栏 | 待填写 | 待填写 | 待填写 | 否 |
| 视频舞台 | 待填写 | 待填写 | 待填写 | 否 |
| 面试官卡片 | 待填写 | 待填写 | 待填写 | 否 |
| 用户卡片 | 待填写 | 待填写 | 待填写 | 否 |
| 右侧面板 | 待填写 | 待填写 | 待填写 | 否 |
| 问题卡片 | 待填写 | 待填写 | 待填写 | 否 |
| 结束按钮 | 待填写 | 待填写 | 待填写 | 否 |

### 第三步：输出计划修改文件

逐个列出：

```text
文件：
修改目的：
修改内容：
风险：
是否修改业务逻辑：
```

### 第四步：输出新的 DOM 结构

先给出简化后的 Vue template 结构，不得直接开始写完整 CSS。

### 第五步：确认后开始修改

只有结构明确后，才开始逐文件修改。

---

## 11. AI 编码规则

### 11.1 布局规则

必须：

- 页面主体使用 Grid；
- 组件内部优先使用 Flex；
- 使用 `min-width: 0`；
- 使用 `min-height: 0`；
- 正确设置局部 `overflow`；
- 使用 `box-sizing: border-box`；
- 视频使用 `aspect-ratio`；
- 使用 `object-fit: cover`；
- 控制内容最大宽度；
- 控制问题卡片最大高度。

禁止：

- 依赖大量 `position: absolute`；
- 使用几十个无解释的 magic number；
- 使用负边距硬拉位置；
- 使用 `transform: translate()` 修正主体布局；
- 使用 `zoom`；
- 使用 `scale`；
- 在模板中加入大量内联样式；
- 复制两套重复 DOM；
- 通过隐藏整个功能绕过布局问题；
- 改动与当前页面无关的全局样式。

### 11.2 Vue 规则

必须：

- 保留现有 `script setup` 或 Options API 风格；
- 保留已有 props、emits 和事件；
- 保留摄像头 `<video>` ref；
- 保留媒体流绑定；
- 保留现有状态；
- UI 组件只接收展示所需数据；
- 不在纯展示组件中重新发起接口请求；
- 不重复创建已有状态。

### 11.3 TypeScript 规则

必须：

- 不使用无意义的 `any`；
- 不删除已有类型；
- 新增类型应就近声明；
- 不为了样式修改重构业务类型；
- 编译必须通过。

---

## 12. 防止 AI “瞎改”的硬性约束

以下规则必须逐条遵守：

1. 不得重新设计页面。
2. 不得改变目标原型中的区域关系。
3. 不得用当前页面布局作为参考继续优化。
4. 不得新增原型中没有的主视觉模块。
5. 不得删除已有业务功能。
6. 不得修改接口和数据结构。
7. 不得一次性重写整个项目。
8. 不得修改其他页面。
9. 不得在没有说明的情况下安装新 UI 框架。
10. 不得将 Element Plus、Ant Design Vue 等组件默认样式直接作为最终视觉。
11. 不得用“差不多”“更现代”“更美观”代替原型还原。
12. 不得只给建议，不修改实际代码。
13. 不得只修改 CSS 而忽略错误 DOM 层级。
14. 不得只修改 DOM 而不处理滚动和尺寸。
15. 不得让浏览器整体滚动条继续存在。
16. 不得出现重复按钮。
17. 不得让长问题撑高页面。
18. 不得把面试官头像放大铺满整个视频区域。
19. 不得把次要设置全部平铺。
20. 不确定时必须提出具体问题，禁止自行猜测。

---

## 13. 分阶段执行方式

为了避免 AI 一次改动过大，严格按照以下阶段执行。

### 阶段一：只做静态骨架

暂时使用静态数据和占位图完成：

- 顶部栏；
- 两列主体；
- 两个视频卡片；
- 右侧面板；
- 问题卡片；
- 状态标签；
- 结束按钮。

验收通过前，不接回复杂交互。

### 阶段二：恢复视频与摄像头

接回：

- 面试官视频或数字人；
- 用户摄像头；
- 麦克风状态；
- 摄像头未开启状态。

接回后不得破坏阶段一布局。

### 阶段三：恢复语音和题目功能

接回：

- 问题数据；
- 语音播报；
- 语音识别；
- 当前回答状态；
- 文本回答模式。

### 阶段四：收纳次要功能

将以下功能放入折叠区域：

- 姿态检测；
- 自动/手动播报；
- 字幕；
- 调试状态；
- 输入方式切换。

### 阶段五：截图对比与微调

在指定分辨率下截图，并逐项对比：

- 位置；
- 大小；
- 间距；
- 对齐；
- 颜色；
- 圆角；
- 滚动；
- 状态；
- 文本行高。

每轮只处理一类差异，禁止同时大改多个区域。

---

## 14. 每轮修改后的强制输出格式

每完成一轮修改，AI 必须输出：

### 14.1 已修改文件

```text
1. 文件路径
   - 修改内容
   - 修改原因
```

### 14.2 保留的业务逻辑

说明以下内容是否保持不变：

- 路由；
- 接口；
- 摄像头；
- 语音识别；
- 问题加载；
- 面试状态；
- 结束面试。

### 14.3 当前完成度

按照以下维度评分：

| 维度 | 当前完成度 | 仍存在的问题 |
|---|---:|---|
| 页面骨架 | 0–100% | |
| 尺寸比例 | 0–100% | |
| 对齐与间距 | 0–100% | |
| 颜色与圆角 | 0–100% | |
| 功能接回 | 0–100% | |
| 响应式 | 0–100% | |

### 14.4 下一步

只列下一轮需要处理的 1 至 3 个明确问题，不得再次大范围重构。

---

## 15. 验收标准

只有同时满足以下要求，才可以宣布完成。

### 15.1 页面结构

- 顶部栏位置正确；
- 左侧视频舞台与右侧面板关系正确；
- 右侧面板宽度稳定；
- 页面在桌面端无整体纵向滚动；
- 页面没有大面积无意义空白；
- 视频区域处于左侧视觉中心。

### 15.2 视频卡片

- 两个视频卡片水平排列；
- 卡片尺寸接近目标原型；
- 卡片比例一致；
- 圆角一致；
- 图片或视频不变形；
- 标签和麦克风按钮位置正确；
- 占位头像没有被拉伸铺满。

### 15.3 右侧面板

- 面试官信息层级正确；
- 当前问题突出；
- 长问题内部滚动；
- 状态标签简洁；
- 结束按钮固定在底部；
- 不存在重复结束按钮；
- 次要功能默认不平铺。

### 15.4 功能

- 页面可正常进入；
- 路由不变；
- 问题正常加载；
- 摄像头正常开启；
- 麦克风正常工作；
- 语音识别正常；
- 播报正常；
- 结束面试正常；
- TypeScript 编译通过；
- 控制台无新增错误。

### 15.5 视觉

在 1440 × 900 分辨率下：

- 页面结构与目标原型高度接近；
- 视频卡片大小不超过原型明显范围；
- 右侧面板不被撑高；
- 颜色和层级接近原型；
- 不出现额外主视觉元素；
- 不出现浏览器纵向滚动条。

---

## 16. AI 首次执行指令

将下面内容作为本次任务的开场指令执行：

```text
请先不要修改任何代码。

你需要阅读我提供的：
1. 目标 UI 原型截图；
2. 当前项目页面截图；
3. 项目中与模拟面试页面相关的代码；
4. 《模拟面试页面 UI 原型精准还原执行文档》。

目标 UI 原型截图是唯一视觉标准，当前项目截图只用于定位差距。

请先完成以下工作：

第一步：找到模拟面试页面的路由、入口组件、子组件、样式文件、摄像头逻辑、语音逻辑、题目展示逻辑和结束面试逻辑。

第二步：输出“原型区域—当前代码—存在问题—计划修改方式”的映射表。

第三步：列出准备修改的文件，并说明每个文件为什么需要修改、是否会影响业务逻辑。

第四步：输出新的页面 DOM 结构和组件结构。

第五步：给出第一阶段的实施计划。第一阶段只做静态页面骨架，不接回复杂功能。

在我确认之前，不要开始修改代码，不要安装新依赖，不要重构无关文件。
```

---

## 17. AI 开始修改时的指令

分析确认后，使用下面指令：

```text
现在开始执行第一阶段。

只完成以下内容：
1. 顶部导航栏；
2. 左侧视频舞台；
3. 两个视频卡片；
4. 右侧面试面板；
5. 面试官信息；
6. 当前问题卡片；
7. 当前状态；
8. 底部结束面试按钮。

要求：
- 保留所有现有业务代码；
- 暂时可以使用现有数据或静态占位内容；
- 页面必须固定在 100vw × 100vh；
- 桌面端禁止出现浏览器整体纵向滚动条；
- 主体使用 Grid；
- 左侧视频区域使用 Flex 居中；
- 两个视频卡片水平排列；
- 右侧面板固定宽度；
- 问题过长时只在问题卡片内部滚动；
- 不显示次要设置；
- 不出现重复按钮；
- 不使用 transform、zoom 或大规模 absolute 定位；
- 不修改其他页面。

修改完成后，请输出：
1. 修改文件列表；
2. 每个文件的修改内容；
3. 保留的业务逻辑；
4. 当前未接回的功能；
5. 编译结果；
6. 仍与目标原型存在的差异。
```

---

## 18. 截图对比后的纠偏指令

当第一轮页面仍有差异时，不要只说“继续优化”，使用以下格式：

```text
请不要重新设计，也不要重构业务逻辑。

根据新截图，只修正以下差异：

1. 左侧视频区域整体偏下，需要回到左侧区域的垂直中心；
2. 两个视频卡片过大，整体最大宽度调整为约 790px；
3. 右侧面板宽度不稳定，固定为约 364px；
4. 问题卡片过高，最大高度限制为 260px，超出内部滚动；
5. 页面存在整体纵向滚动条，检查 html、body、#app、页面根节点和主体高度；
6. 次要功能仍然平铺，请收进默认折叠的“更多设置”；
7. 结束面试按钮重复，只保留底部一个；
8. 面试官占位头像被拉伸铺满，请改为居中显示的 96px 至 128px 头像。

本轮只能修改与以上问题直接相关的文件。
修改后逐条说明每个问题是否已经解决。
```

---

## 19. 最终原则

这次工作的本质不是“让 AI 帮忙美化页面”，而是：

**让 AI 按照明确的视觉标准、固定的页面结构、受控的修改范围和可检查的验收条件，逐步完成 UI 原型还原。**

AI 容易瞎改，通常不是因为它完全无法写前端，而是因为任务中缺少以下内容：

- 哪张图才是标准；
- 什么能改；
- 什么不能改；
- 页面结构是什么；
- 每个区域多大；
- 功能如何收纳；
- 先做什么；
- 后做什么；
- 怎么判断完成；
- 出错时如何纠偏。

本文件已经将这些内容固定下来。执行时不要一次把所有功能和视觉问题混在一起处理，应按照“分析—静态骨架—功能接回—截图对比—局部纠偏”的顺序推进。
